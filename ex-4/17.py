import intro #Use to style and to introduce about the developer of the program


x=input("Enter a string ")
print("Reverse of the string is ",x[::-1])
#the slice statement [::-1] is the same as [11:0:-1] which
#means start at position 11 (because "Hello "World" has 11 characters),
#end at position 0, move with the step -1, negative one, which means one
#step backwards.


import end #Use to give style while ending the program
